import styled from 'styled-components';

export const Container = styled.div`
    display: flex;
    align-items: center;
    gap: 8px;
    margin-bottom: 4px;

    svg {
        color: var(--blue-500);
    }
`;
